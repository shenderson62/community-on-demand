import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    borderRadius: 99999,
    paddingVertical: 16,
    width: '100%',
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center"
  }
});
